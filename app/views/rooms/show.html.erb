<div class="bs-docs-section">

    <div class="row">
      <div class="col-lg-12">
        <div class="page-header">
          <h1 id="tables">メンバー</h1>
        </div>

        <div class="bs-component">
          <table class="table table-striped table-hover ">

            <tbody>
            <% @users.each do |user| %>
              <tr>
                <td><%= user.positon %></td>
                <td><%= user.display_name %></td>
              </tr>
              <% end %>
            </tbody>
          </table>
        </div><!-- /example -->
      </div>
    </div>
  </div>

  
 <br />
